package com.example.notesAPI.controller;

import org.springframework.stereotype.Controller;

@Controller
public class notesController {
//    @GetMapping("/users/{id}")
//    public UserDTO getUser(@PathVariable Long id) {
//        User user = userService.findById(id);
//        return userService.toDTO(user);
//    }
//@PostMapping("/notes")
//public NoteResponseDTO create(@RequestBody CreateNoteDTO dto) {
//    Note newNote = new Note();
//    newNote.setTitle(dto.getTitle());
//    newNote.setContent(dto.getContent());
//
//    Note saved = noteRepository.save(newNote);
//
//    return new NoteResponseDTO(saved.getId(), saved.getTitle(), saved.getContent());
//}

    // HOW TO USE DTO IN CONTROLLER (EXAMPLEs)

}

